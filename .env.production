# Production Environment Configuration
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Database configuration for production (Neon Cloud)
# This should be set as environment variable in your deployment platform
# DATABASE_URL=postgresql://username:password@hostname.neon.tech/database?sslmode=require

# JWT Secret for production (MUST be set as environment variable)
# JWT_SECRET=your_production_jwt_secret_here

# Arcjet for production (set as environment variable)
# ARCJET_KEY=your_production_arcjet_key_here

# Security settings
TRUST_PROXY=1

# Health check settings
HEALTH_CHECK_TIMEOUT=5000

# Logging configuration
LOG_FORMAT=combined
LOG_MAX_FILES=10
LOG_MAX_SIZE=10m